# Testing SQLite JDBC with Encryption

### Set Up
You must add the JDBC jar file that has encryption feature inside the /driver folder.

### Run Tests
in Linux, type
```./gradlew bootRun```

in Windows, type
```gradlew.exe bootRun```